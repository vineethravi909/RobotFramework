language: python
python:
  - "3.7.6"

install:
  - pip install -r requirements.txt
sudo: required
addons:
  chrome: stable

before_script:
  - LATEST_CHROMEDRIVER_VERSION=curl -s "https://chromedriver.storage.googleapis.com/83.0.4103.39/chromedriver_mac64.zip"
  - curl “https://chromedriver.storage.googleapis.com/83.0.4103.39/chromedriver_mac64.zip
  - mkdir geckodriver chromedriver
  - unzip chromedriver_mac64.zip -d chromedriver/
  - export PATH=$PATH:$PWD/chromedriver

script:
  - robot -d results Tests/TestCases.robot

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master